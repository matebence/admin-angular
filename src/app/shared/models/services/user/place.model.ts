export interface Place {
  placeId: number,
  country: string,
  region: string,
  district: string,
  place: string,
  street: string,
  zip: number,
  code: string,
  createdAt: Date,
  updatedAt: Date,
  deletedAt: Date,
  deleted: boolean
}
